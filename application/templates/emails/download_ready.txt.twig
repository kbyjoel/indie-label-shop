{{ 'email.download_ready.header'|trans({}, 'messages', locale) }} — {{ orderNumber }}

{{ 'email.download_ready.intro'|trans({'number': orderNumber, 'format': format|upper}, 'messages', locale) }}

{% if expiresAt %}{{ 'email.download_ready.expires_until'|trans({'date': expiresAt|date('email.download_ready.date_format'|trans({}, 'messages', locale))}, 'messages', locale) }}{% else %}{{ 'email.download_ready.expires'|trans({}, 'messages', locale) }}{% endif %}

{{ 'email.download_ready.cta'|trans({}, 'messages', locale) }} :
{{ signedUrl }}

{{ 'email.download_ready.account_notice'|trans({}, 'messages', locale) }}
