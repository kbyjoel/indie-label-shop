{% import '@AropixelAdmin/Macro/actions.html.twig' as list %}

{{ list.actions(
    item,
    path('admin_product_option_edit', {'id': item.id}),
    path('admin_product_option_delete', {'id': item.id})
) }}

