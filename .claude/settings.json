{
  "permissions": {
    "allow": [
      "Bash(*)"
    ]
  },
  "hooks": {
    "UserPromptSubmit": [
      {
        "hooks": [
          {
            "type": "command",
            "command": "cd '$(git rev-parse --show-toplevel)/application' && if git diff --name-only HEAD 2>/dev/null | grep -q '\\.php$'; then castor qa:phpstan 2>&1 | tail -40; fi"
          }
        ]
      }
    ]
  }
}
